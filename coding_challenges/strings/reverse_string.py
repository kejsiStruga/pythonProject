
def first_reverse(str_param):
    # slicing function takes as param start:end:increment
    # start is inclusive while end is noninclusive
    str_param_new = str_param[::-1]
    str_param = str_param_new

    return str_param
